## 1. 配置与数据结构

- [x] 1.1 在 `app/core/config.py` 中新增 `quality_abnormal_detection` 配置模型，包含检测开关、分析尺寸、阈值和花屏分块参数。
- [x] 1.2 在 `app/core/config.py` 中新增 `occlusion_detection` 配置模型，包含检测开关、后端类型、分析尺寸、遮挡面积阈值和线状遮挡参数。
- [x] 1.3 更新 `config.toml`，新增 `[quality_abnormal_detection]` 与 `[occlusion_detection]` 默认配置段。
- [x] 1.4 新增画面异常 Pydantic schema，覆盖请求、响应、异常明细、错误响应和类型枚举说明。
- [x] 1.5 新增镜头遮挡 Pydantic schema，覆盖请求、响应、面积占比、检测分数和错误响应。

## 2. 共享图像预处理

- [x] 2.1 抽取或复用 Base64 图片解码逻辑，保证画面异常和遮挡接口与现有图片大小限制一致。
- [x] 2.2 实现共享预处理结构，生成 resized BGR、gray、HSV、Lab、基础尺寸信息和可选 overlay 屏蔽区域。
- [x] 2.3 为画面异常检测实现分块工具，支持固定网格切块、异常块 mask 和面积占比计算。
- [x] 2.4 为遮挡检测实现 mask 合并和面积占比工具，保证返回值范围为 `0` 到 `1`。

## 3. 画面异常检测实现

- [x] 3.1 新增 `app/services/quality_abnormal_detector.py`，定义统一检测入口和检测结果数据结构。
- [x] 3.2 实现偏色检测，基于 Lab/RGB/HSV 色彩偏移指标输出 `type=2`、`score` 和中文提示。
- [x] 3.3 实现雪花噪点检测，基于高频噪声、孤立噪点或边缘密度异常输出 `type=3`、`score` 和中文提示。
- [x] 3.4 实现虚焦检测，基于清晰度、梯度或边缘密度指标，并结合雪花噪点状态做修正，输出 `type=1`、`score` 和中文提示。
- [x] 3.5 实现花屏检测初版，基于固定切块、异常块打分、形态学连接、连通域和异常面积比例输出 `type=4`、`score` 和中文提示。
- [x] 3.6 实现画面异常聚合逻辑，保证 `abnormal_types` 只包含命中类型，`results` 只包含对应命中明细。

## 4. 镜头遮挡检测实现

- [x] 4.1 新增 `app/services/occlusion_detector.py`，定义统一检测入口、OpenCV 后端接口和后续 YOLO-seg 后端占位接口。
- [x] 4.2 实现大面积近镜头遮挡候选区域检测，输出候选 mask 和分数。
- [x] 4.3 实现线状遮挡候选区域检测，覆盖电线/绳子等细长近景遮挡。
- [x] 4.4 实现近景模糊遮挡候选区域检测，用于辅助识别贴近镜头的模糊遮挡物。
- [x] 4.5 合并遮挡候选 mask，清理噪声区域，计算 `occlusion_area_ratio`。
- [x] 4.6 实现遮挡判定逻辑，输出 `is_occluded`、`occlusion_area_ratio`、`score` 和中文 `message`，第一版不输出遮挡物枚举。

## 5. API 路由与集成

- [x] 5.1 新增 `app/api/v1/quality_abnormal.py`，实现 `/detect_quality_abnormal` 路由和错误处理。
- [x] 5.2 新增 `app/api/v1/occlusion.py`，实现 `/detect_occlusion` 路由和错误处理。
- [x] 5.3 更新 `app/api/v1/router.py`，挂载画面异常和镜头遮挡路由，支持无前缀和 `/api/v1` 双路径。
- [x] 5.4 更新 `/config` 响应，返回新增配置段；确认 `/config/reload` 能热重载新增阈值配置。
- [x] 5.5 确认新接口日志包含请求耗时、状态码和 `X-Request-ID`。

## 6. 文档与验收

- [x] 6.1 更新 `docs/API接口文档.md`，用中文补充两个新接口的请求、响应、枚举、错误码和示例。
- [x] 6.2 更新 `README.md` 和 `AGENT.md`，说明新能力、配置、样例目录和部署注意事项。
- [x] 6.3 新增或扩展验收脚本，针对 `test/图像检测/画面异常` 验证虚焦、偏色、雪花噪点和花屏响应。
- [x] 6.4 新增或扩展验收脚本，针对 `test/图像检测/遮挡` 验证镜头遮挡响应和面积占比输出。
- [x] 6.5 记录花屏初版验收口径，目标为明显花屏样例约 70% 准确率。
- [x] 6.6 在文档中记录 YOLO-seg 遮挡后端的数据量建议、单类 `occlusion` 标注要求和 YOLO segmentation 数据格式。

## 7. 验证

- [x] 7.1 运行画面异常服务层单图验证，确认四类异常样例至少能返回预期类型。
- [x] 7.2 运行遮挡服务层单图验证，确认遮挡样例返回 `is_occluded=true` 且面积占比大于阈值。
- [x] 7.3 运行正常样例负例验证，确认正常图不应大量误判为画面异常或镜头遮挡。
- [x] 7.4 启动 FastAPI 服务并通过 HTTP 调用两个新接口，确认无前缀和 `/api/v1` 路径均可用。
- [x] 7.5 执行现有倾斜检测和屏幕检测验收，确认新增能力未破坏现有接口。
