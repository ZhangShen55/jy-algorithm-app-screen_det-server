# Docker 部署依赖（配合 Dockerfile 基础镜像 pytorch/pytorch:2.6.0-cuda11.8-cudnn9-runtime）
# torch 由基础镜像预装（2.6.0+cu118），勿在此声明
# ultralytics 在 Dockerfile 中 --no-deps 安装，避免 pip 覆盖 torch

fastapi==0.111.0
uvicorn[standard]==0.30.1
opencv-python-headless==4.10.0.84
numpy==1.26.4
pillow==10.4.0
pydantic==2.8.2
python-multipart==0.0.9
psutil==6.0.0
toml==0.10.2
matplotlib>=3.3.0
pyyaml>=5.3.1
scipy>=1.4.1
requests>=2.23.0
tqdm>=4.64.0
cryptography>=42.0.0,<46
